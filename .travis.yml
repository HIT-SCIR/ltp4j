language:
    - cpp
    - java

os:
    - linux
    - osx

before_script:
    - if [[ -z "$JAVA_HOME" && -x "/usr/libexec/java_home" ]] ; then export JAVA_HOME=$(/usr/libexec/java_home); fi
    - git submodule init
    - git submodule update

script: mvn

